/*
03)Crie uma função que recebe dois parâmetros, base e expoente, e
retorne a base elevada ao expoente.
*/

function potenciacao(base, expoente) {
    return base ** expoente // Math.pow(x, y) seria o jeito antigo de fazer isso
}

// execução
console.log(potenciacao(3, 2))
